@extends('backend.layouts.app')
@section('content')
<div class="container-fluid"><div class="d-flex justify-content-between mb-4"><h3>Blog & News</h3><a class="btn btn-primary" href="{{route('admin.news.create')}}">Create Article</a></div>@if(session('message'))<div class="alert alert-success">{{session('message')}}</div>@endif
<div class="card mb-4"><div class="card-body"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Article</th><th>Category</th><th>Author</th><th>Status</th><th>Published</th><th>Views</th><th></th></tr></thead><tbody>@forelse($posts as $post)<tr><td><strong>{{$post->title}}</strong></td><td>{{$post->category?->name??'—'}}</td><td>{{$post->author}}</td><td><span class="badge {{$post->status==='published'?'bg-success':'bg-secondary'}}">{{ucfirst($post->status)}}</span></td><td>{{$post->published_at?->format('M j, Y')??'—'}}</td><td>{{$post->view_count}}</td><td><a class="btn btn-sm btn-primary" href="{{route('admin.news.edit',$post)}}">Edit</a> <form class="d-inline" method="POST" action="{{route('admin.news.destroy',$post)}}">@csrf @method('DELETE')<button class="btn btn-sm btn-danger" onclick="return confirm('Delete article?')">Delete</button></form></td></tr>@empty<tr><td colspan="7" class="text-center">No articles.</td></tr>@endforelse</tbody></table></div>{{$posts->links()}}</div></div>
<div class="row g-4">@foreach(['category'=>['label'=>'Categories','items'=>$categories],'tag'=>['label'=>'Tags','items'=>$tags]] as $type=>$group)<div class="col-lg-6"><div class="card"><div class="card-header"><h4>{{$group['label']}}</h4></div><div class="card-body"><form method="POST" action="{{route('admin.news.taxonomy.save',$type)}}" class="row g-2 mb-3">@csrf<div class="col"><input name="name" class="form-control" placeholder="New {{ $type }} name" required></div><div class="col-auto"><button class="btn btn-success">Add</button></div></form><ul class="list-group">@foreach($group['items'] as $item)<li class="list-group-item d-flex justify-content-between">{{$item->name}} <span>{{$item->posts_count}} posts <form class="d-inline" method="POST" action="{{route('admin.news.taxonomy.delete',[$type,$item->id])}}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></span></li>@endforeach</ul></div></div></div>@endforeach</div></div>
@endsection
