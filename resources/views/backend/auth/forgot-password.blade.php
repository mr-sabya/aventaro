@extends('backend.layouts.guest') @section('content')<div class="card p-4" style="max-width:520px;margin:auto"><h3>Reset password</h3><p>Enter your administrator email to receive a secure reset link.</p>@if(session('status'))<div class="alert alert-success">{{session('status')}}</div>@endif<form method="POST" action="{{route('admin.password.email')}}">@csrf<label class="form-label">Email</label><input type="email" name="email" value="{{old('email')}}" class="form-control @error('email') is-invalid @enderror" required>@error('email')<div class="invalid-feedback">{{$message}}</div>@enderror<button class="btn btn-primary mt-3">Send reset link</button></form></div>@endsection
