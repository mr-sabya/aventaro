<div class="container-fluid"><h3 class="mb-4">Contact Inbox</h3>@if(session('message'))<div class="alert alert-success">{{session('message')}}</div>@endif<div class="row g-4"><div class="col-lg-7"><div class="card"><div class="card-body"><div class="row g-2 mb-3"><div class="col-6"><input class="form-control" wire:model.live.debounce.300ms="search" placeholder="Search messages"></div><div class="col-3"><select class="form-select" wire:model.live="status"><option value="all">All status</option><option value="new">New</option><option value="read">Read</option><option value="replied">Replied</option></select></div><div class="col-3"><select class="form-select" wire:model.live="type"><option value="all">All types</option><option value="contact">Contact</option><option value="appointment">Appointment</option></select></div></div><div class="list-group">@forelse($messages as $item)<button type="button" wire:click="open({{$item->id}})" class="list-group-item list-group-item-action {{$item->status==='new'?'fw-bold':''}}"><div class="d-flex justify-content-between"><span>{{$item->name}} — {{$item->subject?:ucfirst($item->type)}}</span><small>{{$item->created_at->diffForHumans()}}</small></div><small>{{$item->email}} · {{ucfirst($item->status)}}</small></button>@empty<div class="text-center py-4">No messages found.</div>@endforelse</div><div class="mt-3">{{$messages->links()}}</div></div></div></div><div class="col-lg-5">@if($selected)<div class="card"><div class="card-header"><h4>{{$selected->subject?:ucfirst($selected->type).' Message'}}</h4></div><div class="card-body"><p><strong>{{$selected->name}}</strong><br><a href="mailto:{{$selected->email}}">{{$selected->email}}</a><br>{{$selected->phone}}</p><hr><p>{!!nl2br(e($selected->message))!!}</p><a class="btn btn-primary" href="mailto:{{$selected->email}}">Reply by Email</a> <button class="btn btn-success" wire:click="markReplied({{$selected->id}})">Mark Replied</button> <button class="btn btn-danger" wire:click="delete({{$selected->id}})" wire:confirm="Delete message?">Delete</button></div></div>@else<div class="card"><div class="card-body text-center py-5">Select a message to read it.</div></div>@endif</div></div></div>
